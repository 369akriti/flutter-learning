class NewList {
  var noteID;
  var noteTitle;
  var noteDateTime;
  var latestEditDateTime;

  NewList(
      {this.noteID,
      this.noteTitle,
      this.noteDateTime,
      this.latestEditDateTime});
}
